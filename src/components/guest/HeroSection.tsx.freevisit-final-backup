import { useEffect, useState } from "react";
import { Link } from "react-router-dom";
import { ArrowRight, Leaf, Trees, ShieldCheck, CheckCircle2, ChevronLeft, ChevronRight } from "lucide-react";

export default function HeroSection() {
  const heroImages = [
    "/images/projects/slide01.jpg",
    "/images/projects/slide02.png",
    "/images/projects/slide03.jpg",
    "/images/projects/slide04.jpg",
    "/images/projects/slide05.jpg",
    "/images/projects/slide06.jpg",
    "/images/projects/slide07.jpg",
    "/images/projects/slide08.jpg",
    "/images/projects/slide09.jpg",
  ];

  const [currentSlide, setCurrentSlide] = useState(0);

  useEffect(() => {
    const timer = setInterval(() => {
      setCurrentSlide((prev) => (prev + 1) % heroImages.length);
    }, 4000);

    return () => clearInterval(timer);
  }, []);
  return (
    <section className="relative overflow-hidden bg-white pt-20">
      {/* Hero Section Container */}
      <div className="mx-auto max-w-7xl px-4 py-8 lg:px-8">
        
        {/* Main Banner / Hero Image at the very top */}
        <div className="relative mb-8 overflow-hidden rounded-3xl shadow-xl">
          <div className="absolute inset-0 bg-gradient-to-t from-black/70 via-black/30 to-transparent z-10" />
          <img
            src={heroImages[currentSlide]}
            alt="Munder Garden"
            className="h-[380px] w-full object-cover sm:h-[460px] lg:h-[520px]"
          />
            <button
              type="button"
              onClick={() => setCurrentSlide((prev) => (prev - 1 + heroImages.length) % heroImages.length)}
              aria-label="Previous project"
              className="absolute left-3 top-1/2 z-30 -translate-y-1/2 rounded-full bg-black/35 p-2 text-white backdrop-blur-sm transition hover:bg-black/55"
            >
              <ChevronLeft size={28} />
            </button>
            <button
              type="button"
              onClick={() => setCurrentSlide((prev) => (prev + 1) % heroImages.length)}
              aria-label="Next project"
              className="absolute right-3 top-1/2 z-30 -translate-y-1/2 rounded-full bg-black/35 p-2 text-white backdrop-blur-sm transition hover:bg-black/55"
            >
              <ChevronRight size={28} />
            </button>
          <div className="absolute bottom-3 left-1/2 z-30 flex -translate-x-1/2 gap-2">
            {heroImages.map((_, index) => (
              <button
                key={index}
                type="button"
                aria-label={`Go to project ${index + 1}`}
                onClick={() => setCurrentSlide(index)}
                className={`h-2.5 rounded-full transition-all ${currentSlide === index ? "w-7 bg-white" : "w-2.5 bg-white/60"}`}
              />
            ))}
          </div>
          <div className="absolute bottom-6 left-6 right-6 z-20 text-white">
            <span className="inline-flex items-center rounded-full bg-green-600/90 px-3.5 py-1.5 text-xs font-semibold sm:text-sm">
              🌿 Professional Gardening Services
            </span>
            <h1 className="mt-3 text-3xl font-extrabold leading-tight sm:text-5xl lg:text-6xl">
              Bring Your Dream Garden to Life
            </h1>
            <p className="mt-2 max-w-xl text-sm text-gray-200 sm:text-base">
              Expert garden care, custom landscaping, and smart solutions delivered right to your doorstep.
            </p>
          </div>
        </div>

          {/* Book Free Visit */}
          <div className="relative overflow-hidden rounded-[24px] bg-[#075B32] px-5 py-6 text-white shadow-xl sm:px-7 sm:py-7">

            {/* Decorative background */}
            <div className="pointer-events-none absolute -right-16 -top-16 h-40 w-40 rounded-full bg-[#20A95A]/20" />
            <div className="pointer-events-none absolute -bottom-20 -right-8 h-48 w-48 rounded-full bg-[#20A95A]/15" />

            {/* Main content */}
            <div className="relative z-10 flex items-center gap-5">

              {/* 100% FREE */}
              <div className="flex h-[112px] w-[92px] shrink-0 flex-col items-center justify-center rounded-[20px] bg-[#D9F000] text-center shadow-lg">
                <span className="text-[30px] leading-none">🎁</span>
                <span className="mt-2 text-[16px] font-extrabold leading-tight text-[#075B32]">
                  100%<br />
                  FREE
                </span>
              </div>

              {/* Content */}
              <div className="min-w-0 flex-1">
                <h2 className="text-[27px] font-extrabold leading-tight sm:text-4xl">
                  <span className="text-[#D9F000]">FREE</span>{" "}
                  <span className="text-white">GARDEN VISIT</span>
                </h2>

                <p className="mt-2 text-[14px] leading-5 text-white sm:text-base">
                  Book your free visit &amp; get expert advice from our specialists.
                </p>

                <div className="mt-3 flex flex-wrap gap-x-3 gap-y-1 text-[12px] font-semibold text-white sm:text-sm">
                  <span>✓ No Cost</span>
                  <span>✓ No Obligation</span>
                  <span>✓ Expert Advice</span>
                </div>
              </div>

            </div>

            {/* Book button */}
            <Link
              to="/contact"
              className="relative z-10 mt-5 flex h-[58px] w-full items-center rounded-[18px] bg-white px-3 font-bold text-[#075B32] shadow-md"
            >
              <span className="flex-1 text-center text-[20px] sm:text-2xl">
                BOOK FREE VISIT
              </span>

              <span className="flex h-[46px] w-[46px] shrink-0 items-center justify-center rounded-full bg-[#075B32] text-white">
                <ArrowRight size={25} />
              </span>
            </Link>

          </div>

      </div>
    </section>
  );
}
